services:
  - type: web
    name: despedida-soltero
    env: node
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: RENDER
        value: true
      - key: RESET_KEY
        value: despedida2025
